---
phase: 08-responsive-table-ui
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/ui/ResponsiveTable.tsx
  - client/src/hooks/useResponsiveTable.ts
  - client/src/types/table.ts
  - client/src/pages/units/Units.tsx
  - client/src/pages/plasmas/Plasmas.tsx
  - client/src/pages/cycles/Cycles.tsx
  - client/src/pages/feed/FeedStock.tsx
  - client/src/pages/reports/StockResume.tsx
  - client/src/pages/admin/Users.tsx
  - client/src/pages/admin/AuditLog.tsx
  - client/src/pages/rbac/RbacManager.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - All DataGrid components replaced with responsive table implementation
    - Table works well on mobile devices with touch-friendly controls
    - Table supports sorting, filtering, and pagination
    - Table adapts to available screen space
  artifacts:
    - path: client/src/components/ui/ResponsiveTable.tsx
      provides: Reusable responsive table component
      min_lines: 100
    - path: client/src/hooks/useResponsiveTable.ts
      provides: Custom hook for table state management
      min_lines: 50
    - path: client/src/types/table.ts
      provides: TypeScript types for table functionality
      min_lines: 20
---

<objective>
Replace all MUI DataGrid components with a modern, responsive table implementation that works well on mobile devices and provides excellent UI/UX.

Purpose: Improve table usability across all screen sizes, especially mobile devices
Output: Responsive table component and hooks replacing MUI DataGrid in all pages
</objective>

<context>
@.planning/phases/08-responsive-table-ui/08-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Problem Statement:**
- Current MUI DataGrid UI looks "jadul" (outdated/old-fashioned)
- Not responsive on mobile phones (HP)
- Need to replace with modern table library that provides good UX on all devices

**Recommended Library:**
- TanStack Table v8 - headless, fully customizable, excellent mobile support
- Alternative: AG Grid, Material Table, or custom implementation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research and select table library</name>
  <files>
    - (none)
  </action>
  Research and select the best table library for the project:
  
  1. Check current package.json for existing table dependencies
  2. Research TanStack Table v8 as primary option (headless, React 18 compatible)
  3. Compare with alternatives: AG Grid, Material Table, react-table
  4. Select based on: mobile responsiveness, customization, bundle size, React 18 support
  5. Document decision in context file
  
  Recommendation: TanStack Table v8 with @tanstack/react-table
</task>
<verify>
  <automated>test -f client/package.json && grep -q "tanstack" client/package.json || echo "Need to install"</automated>
</verify>
<done>
  - Table library selected and installed
  - Decision documented
</done>
</task>

<task type="auto">
  <name>Task 2: Create base responsive table component</name>
  <files>
    - client/src/components/ui/ResponsiveTable.tsx
  </action>
  Create a reusable responsive table component with the following features:
  
  1. **Mobile-responsive design:**
     - Horizontal scroll for overflow columns on mobile
     - Collapsible columns based on priority
     - Touch-friendly pagination controls
     - Larger touch targets (44px minimum)
  
  2. **Core functionality:**
     - Column sorting (asc/desc)
     - Pagination with configurable page size
     - Loading state with skeleton
     - Empty state with message
     - Row selection (optional)
  
  3. **Visual design:**
     - Clean, modern look matching project theme
     - Consistent with existing UI patterns
     - Zebra striping for rows
     - Clear header styling
  
  4. **API design:**
     ```tsx
     <ResponsiveTable
       data={data}
       columns={columns}
       loading={isLoading}
       pagination={{ page, pageSize, total, onPageChange, onPageSizeChange }}
       sorting={{ column, direction, onSort }}
       onRowClick={(row) => void}
     />
     ```
</task>
<verify>
  <automated>test -f client/src/components/ui/ResponsiveTable.tsx</automated>
</verify>
<done>
  - ResponsiveTable component created
  - Exports properly for use in pages
</done>
</task>

<task type="auto">
  <name>Task 3: Create table types and hooks</name>
  <files>
    - client/src/types/table.ts
    - client/src/hooks/useResponsiveTable.ts
  </action>
  Create TypeScript types and custom hooks for table functionality:
  
  1. **Types (client/src/types/table.ts):**
     - Column definition type with alignment, width, priority for mobile
     - Pagination state type
     - Sorting state type
     - Table props type
  
  2. **Hook (client/src/hooks/useResponsiveTable.ts):**
     - State management for sorting, pagination, selection
     - Computed properties for pagination (hasNext, hasPrev, totalPages)
     - Event handlers for user interactions
</task>
<verify>
  <automated>test -f client/src/types/table.ts && test -f client/src/hooks/useResponsiveTable.ts</automated>
</verify>
<done>
  - Table types defined and exported
  - useResponsiveTable hook created
</done>
</task>

<task type="auto">
  <name>Task 4: Replace DataGrid in Units page</name>
  <files>
    - client/src/pages/units/Units.tsx
  </action>
  Replace MUI DataGrid with ResponsiveTable in Units.tsx:
  
  1. Import ResponsiveTable and types
  2. Convert column definitions to new format
  3. Replace DataGrid usage with ResponsiveTable
  4. Map data to new format
  5. Test sorting, pagination, and display
</task>
<verify>
  <automated>grep -c "ResponsiveTable" client/src/pages/units/Units.tsx</automated>
</verify>
<done>
  - Units.tsx uses ResponsiveTable
  - Functionality preserved
</done>
</task>

<task type="auto">
  <name>Task 5: Replace DataGrid in Plasmas page</name>
  <files>
    - client/src/pages/plasmas/Plasmas.tsx
  </action>
  Replace MUI DataGrid with ResponsiveTable in Plasmas.tsx following the same pattern as Units.
</task>
<verify>
  <automated>grep -c "ResponsiveTable" client/src/pages/plasmas/Plasmas.tsx</automated>
</verify>
<done>
  - Plasmas.tsx uses ResponsiveTable
</done>
</task>

<task type="auto">
  <name>Task 6: Replace DataGrid in Cycles page</name>
  <files>
    - client/src/pages/cycles/Cycles.tsx
  </action>
  Replace MUI DataGrid with ResponsiveTable in Cycles.tsx following the same pattern.
</task>
<verify>
  <automated>grep -c "ResponsiveTable" client/src/pages/cycles/Cycles.tsx</automated>
</verify>
<done>
  - Cycles.tsx uses ResponsiveTable
</done>
</task>

<task type="auto">
  <name>Task 7: Replace DataGrid in remaining pages</name>
  <files>
    - client/src/pages/feed/FeedStock.tsx
    - client/src/pages/reports/StockResume.tsx
    - client/src/pages/admin/Users.tsx
    - client/src/pages/admin/AuditLog.tsx
    - client/src/pages/rbac/RbacManager.tsx
  </action>
  Replace MUI DataGrid with ResponsiveTable in all remaining pages:
  - FeedStock.tsx
  - StockResume.tsx
  - Users.tsx
  - AuditLog.tsx
  - RbacManager.tsx (both Roles and Users tabs)
</task>
<verify>
  <automated>grep -c "ResponsiveTable" client/src/pages/feed/FeedStock.tsx client/src/pages/reports/StockResume.tsx client/src/pages/admin/Users.tsx client/src/pages/admin/AuditLog.tsx client/src/pages/rbac/RbacManager.tsx</automated>
</verify>
<done>
  - All pages use ResponsiveTable
</done>
</task>

<task type="auto">
  <name>Task 8: Verify mobile responsiveness</name>
  <files>
    - (none - manual testing)
  </action>
  Verify the responsive behavior:
  
  1. Test table renders correctly on mobile viewport
  2. Verify horizontal scroll works for overflow
  3. Test touch interactions (swipe, tap)
  4. Verify pagination controls work on mobile
  5. Check no layout overflow issues
</task>
<verify>
  <automated>echo "Manual verification required for mobile responsiveness"</automated>
</verify>
<done>
  - Mobile responsiveness verified
</done>
</task>

</tasks>

<success_criteria>
- All 9 pages using DataGrid replaced with ResponsiveTable component
- Responsive table component is mobile-friendly and adaptive
- Original table functionality (sort, filter, paginate) preserved
- No regression in existing functionality
- Component follows established patterns from codebase
</success_criteria>

<output>
After completion, create `.planning/phases/08-responsive-table-ui/08-SUMMARY.md`