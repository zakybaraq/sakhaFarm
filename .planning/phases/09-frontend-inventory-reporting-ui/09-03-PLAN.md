---
phase: 09-frontend-inventory-reporting-ui
plan: 03
tags: [frontend, admin, rbac, audit, recording]
wave: 1
date: "2026-04-17"
---

# Phase 9 Plan 03: Admin & RBAC UI

## Wave Structure

### Wave 1 — Core Admin Features
1. **Daily Recording Form** — Live FCR/IP/deviation calculations
2. **Audit Log Viewer** — Admin-only searchable DataGrid
3. **RBAC Manager** — Single page with tabs

### Wave 2 — Advanced Features
- Role-permission matrix
- User management CRUD
- Audit log filtering

### Wave 3 — Polish
- Permission checks for UI elements
- Empty states
- Loading states

---

## Wave 1 Tasks

### T-07: Daily Recording Form

**File:** `client/src/pages/recordings/DailyRecording.tsx`

**Task:**
- Create form with fields: date, cycleId, dead, culled, remainingPopulation, bodyWeight
- **Live calculations displayed:**
  - Cumulative mortality %
  - SR (Survival Rate) = (remaining / initial) * 100
  - Deviation from standard BW (fetched from `/api/standards`)
  - Running FCR (if feed data available)
  - Projected IP at harvest
- Submit to `/api/recordings` (POST)

**Acceptance:**
- [ ] Form calculates metrics in real-time as user types
- [ ] Standard BW deviation shown (+/- grams)
- [ ] Submit creates daily recording
- [ ] Validation prevents future dates

---

### T-08: Audit Log Viewer

**File:** `client/src/pages/admin/AuditLog.tsx`

**Task:**
- Create admin-only page (check permission: 'audit:read')
- DataGrid with columns: timestamp, user, action, entity, details
- Filters: date range, action type, user
- Search: free-text search across action/details
- Fetch from `/api/audit/logs` (GET)

**Acceptance:**
- [ ] DataGrid shows audit entries
- [ ] Filters work (date, action, user)
- [ ] Search filters results
- [ ] Non-admin users cannot access (403)

---

### T-09: RBAC Manager Page

**File:** `client/src/pages/rbac/RbacManager.tsx`

**Task:**
- Create single page with MUI Tabs: Roles, Permissions, Users
- **Roles Tab:**
  - DataGrid: role name, description, user count, permission count
  - Create/Edit role dialog
  - Permission matrix (checkbox grid)
- **Permissions Tab:**
  - Read-only list of all permissions by category
- **Users Tab:**
  - DataGrid: name, email, role, tenant, status
  - Create/Edit user dialog
  - Deactivate/activate toggle

**Acceptance:**
- [ ] Tabs switch between Roles/Permissions/Users
- [ ] Roles DataGrid with CRUD operations
- [ ] Permission matrix for role editing
- [ ] Users DataGrid with CRUD operations

---

## Dependencies

- Phase 8: Auth context, permissions
- API: `/api/recordings`, `/api/audit/logs`, `/api/rbac/*`, `/api/users`
- UI-SPEC.md: Tabs, DataGrid, Dialog specs

---

## PLAN COMPLETE