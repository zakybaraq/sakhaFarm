---
phase: 05-data-integration
plan: '03'
type: execute
wave: 2
depends_on: []
files_modified:
  - client/src/pages/admin/AuditLog.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - AuditLog shows real audit data from API
  artifacts:
    - path: client/src/pages/admin/AuditLog.tsx
      provides: Audit log with live data
      contains: useQuery.*audit
---

<objective>
Connect AuditLog page to API - replace DUMMY_AUDIT with real audit data.
</objective>

<context>
@.planning/phases/05-data-integration/5-CONTEXT.md
@client/src/pages/admin/AuditLog.tsx

Check if /api/audit endpoint exists in server/src/modules/rbac/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check audit API endpoint</name>
  <files>server/src/modules/rbac/</files>
  <action>
1. First check if audit API endpoint exists:
   - Look for /api/audit routes
   - If not exists, this plan needs backend work first

2. If exists, create client/src/api/audit.ts with:
   - listAuditLogs(filters) API function

3. Connect AuditLog.tsx to use the API
</action>
<verify>
<automated>grep -q "api/audit" client/src/pages/admin/AuditLog.tsx</automated>
</verify>
<done>
  - AuditLog shows real data
</done>
</task>

</tasks>

<success_criteria>
- If backend has /api/audit - connect to it
- If not - document as blocker
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-integration/05-03-SUMMARY.md`
</output>