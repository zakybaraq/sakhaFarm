---
phase: 05-data-integration
plan: '02'
type: execute
wave: 2
depends_on: []
files_modified:
  - client/src/pages/recordings/DailyRecording.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - DailyRecording cycle dropdown uses real data from /api/cycles?status=active
    - Standards come from backend or are static lookup
  artifacts:
    - path: client/src/pages/recordings/DailyRecording.tsx
      provides: Recording form with real cycle data
      contains: useQuery.*cycles
---

<objective>
Connect DailyRecording page to API - replace mockCycles with real active cycles from backend.
</objective>

<context>
@.planning/phases/05-data-integration/5-CONTEXT.md
@.planning/phases/05-data-integration/5-RESEARCH.md
@client/src/pages/recordings/DailyRecording.tsx
@client/src/api/cycles.ts

# Existing API functions
From client/src/api/cycles.ts:
- listCycles() - list all cycles
- listActiveCycles(tenantId) - list active cycles only
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace mockCycles with API</name>
  <files>client/src/pages/recordings/DailyRecording.tsx</files>
  <read_first>client/src/pages/recordings/DailyRecording.tsx, client/src/api/cycles.ts</read_first>
  <action>
1. Add imports:
```typescript
import { useQuery } from '@tanstack/react-query'
import { listActiveCycles } from '../../api/cycles'
import { useAuth } from '../../contexts/AuthContext'
```

2. Remove mock data:
```typescript
// Remove these lines (around line 33-42):
const mockCycles = [...]
const mockStandards = ...
```

3. Add state and query after component:
```typescript
const { user } = useAuth()

const { data: cyclesData, isLoading: cyclesLoading } = useQuery({
  queryKey: ['cycles', 'active', user?.tenantId],
  queryFn: () => user?.tenantId ? listActiveCycles(user.tenantId) : Promise.resolve([]),
  enabled: !!user?.tenantId,
})
```

4. Replace the cycle dropdown mapping:
```typescript
// Change from:
{/* mockCycles.map(...) */}

// To:
{cyclesData?.cycles.map((cycle: { id: number; cycleNumber: number; docType: string }) => (
  <MenuItem key={cycle.id} value={cycle.id}>
    Cycle {cycle.cycleNumber} ({cycle.docType})
  </MenuItem>
))}
```

5. Update standards - for now keep as static (Standards API not available yet)
</action>
<verify>
<automated>grep -q "listActiveCycles" client/src/pages/recordings/DailyRecording.tsx</automated>
</verify>
<done>
  - Cycle dropdown shows real active cycles
  - Uses /api/cycles?status=active
</done>
</task>

</tasks>

<success_criteria>
- DailyRecording cycle dropdown uses listActiveCycles from API
- User can select from real active cycles
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-integration/05-02-SUMMARY.md`
</output>