---
phase: 06-feed-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/db/schema/feed_surat_jalan.ts
  - server/src/db/schema/feed_products.ts
  - server/src/modules/feed/feed.errors.ts
  - server/src/modules/feed/feed.service.ts
  - server/src/modules/feed/feed.controller.ts
  - server/src/modules/feed/feed.routes.ts
autonomous: true
requirements:
  - FR-6
  - FR-7
user_setup: []

must_haves:
  truths:
    - Feed products can be created, read, updated, soft-deleted
    - Surat Jalan increases feed stock for a plasma
    - Stock is tracked in both Zak and Kg units
    - All movements logged with audit trail
  artifacts:
    - path: server/src/db/schema/feed_surat_jalan.ts
      provides: Surat Jalan delivery records
      contains: feed_surat_jalan table
    - path: server/src/modules/feed/feed.errors.ts
      provides: Feed-specific error classes
    - path: server/src/modules/feed/feed.service.ts
      provides: Feed CRUD + Surat Jalan business logic
    - path: server/src/modules/feed/feed.controller.ts
      provides: All feed API endpoints
    - path: server/src/modules/feed/feed.routes.ts
      provides: Route exports
  key_links:
    - from: server/src/modules/feed/feed.service.ts
      to: server/src/db/schema/feed_products
      via: Drizzle ORM queries
      pattern: db.select.+feedProducts
    - from: server/src/modules/feed/feed.service.ts
      to: server/src/db/schema/feed_stock
      via: Drizzle ORM with transaction
      pattern: db.update\(feedStock\)
    - from: server/src/modules/feed/feed.controller.ts
      to: server/src/plugins/rbac
      via: requirePermission guard
      pattern: requirePermission\('feed
---

<objective>
Implement feed product master CRUD and Surat Jalan (feed delivery) endpoint to enable feed stock tracking.
</objective>

<context>
@server/src/db/schema/feed_products.ts
@server/src/db/schema/feed_stock.ts
@server/src/db/schema/feed_movements.ts
@server/src/modules/recordings/recordings.errors.ts
@server/src/modules/recordings/recordings.service.ts
@server/src/modules/recordings/recordings.controller.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create feed_surat_jalan table schema</name>
  <files>server/src/db/schema/feed_surat_jalan.ts</files>
  <read_first>
    - server/src/db/schema/feed_movements.ts (reference pattern)
    - server/src/db/schema/feed_products.ts (FK reference)
  </read_first>
  <action>
Create new schema file `server/src/db/schema/feed_surat_jalan.ts`:
- Table name: `feed_surat_jalan`
- Columns:
  - id: int().autoincrement().primaryKey()
  - plasmaId: int() NOT NULL references(plasmas.id)
  - feedProductId: int() NOT NULL references(feedProducts.id)
  - suratJalanNumber: varchar(50) NOT NULL (unique)
  - vendor: varchar(100) NOT NULL
  - deliveryDate: date NOT NULL
  - vehicleNumber: varchar(20) (optional)
  - driverName: varchar(100) (optional)
  - totalZak: decimal(10,3) NOT NULL
  - totalKg: decimal(10,3) NOT NULL
  - notes: text (optional)
  - createdBy: varchar(16) references(users.id)
  - createdAt: timestamp().defaultNow()
- Indexes: idx_surat_jalan_number, idx_surat_jalan_plasma
</action>
  <verify>
    <automated>grep -n "feed_surat_jalan" server/src/db/schema/index.ts</automated>
  </verify>
  <done>feed_surat_jalan schema exists, exported from schema/index.ts</done>
</task>

<task type="auto">
  <name>Task 2: Add zakKgConversion to feed_products</name>
  <files>server/src/db/schema/feed_products.ts</files>
  <read_first>
    - server/src/db/schema/feed_products.ts
  </read_first>
  <action>
Modify `server/src/db/schema/feed_products.ts`:
- Add `zakKgConversion: decimal(10,2).default('50')` column after defaultUnit
- This defines how many kg per Zak for each feed type (default 50kg)
</action>
  <verify>
    <automated>grep -n "zakKgConversion" server/src/db/schema/feed_products.ts</automated>
  </verify>
  <done>feed_products has zakKgConversion column</done>
</task>

<task type="auto">
  <name>Task 3: Create feed.errors.ts and feed.service.ts (CRUD + Surat Jalan)</name>
  <files>server/src/modules/feed/feed.errors.ts, server/src/modules/feed/feed.service.ts</files>
  <read_first>
    - server/src/modules/recordings/recordings.errors.ts (error pattern)
    - server/src/modules/recordings/recordings.service.ts (service pattern)
    - server/src/db/schema/feed_products.ts
    - server/src/db/schema/feed_stock.ts
    - server/src/db/schema/feed_movements.ts
    - server/src/db/schema/feed_surat_jalan.ts (new)
  </read_first>
  <action>
Create error classes in `server/src/modules/feed/feed.errors.ts`:
- FeedProductNotFoundError(id)
- FeedStockNotFoundError(plasmaId, feedProductId)
- DuplicateFeedCodeError(code)
- NegativeStockError(current, requested)
- InvalidSuratJalanError(number)

Create service functions in `server/src/modules/feed/feed.service.ts`:
- createFeedProduct(input, tenantId, userId): Insert feed_products, return new id
- listFeedProducts(tenantId): List all active feed products
- getFeedProduct(id, tenantId): Get single product
- updateFeedProduct(id, input, tenantId, userId): Update product
- softDeleteFeedProduct(id, tenantId, userId): Soft delete (set deletedAt)
- createSuratJalan(input, tenantId, userId): Record delivery, update stock, create movement record within transaction
  - Steps: 1) Create feed_surat_jalan record, 2) Insert feed_movement (type='in'), 3) Update feed_stock (increment totalInKg)
  - Transaction-wrapped: use db.transaction()
</action>
  <verify>
    <automated>ls server/src/modules/feed/feed.errors.ts server/src/modules/feed/feed.service.ts</automated>
  </verify>
  <done>feed.errors.ts has 5 error classes, feed.service.ts has CRUD + Surat Jalan functions</done>
</task>

<task type="auto">
  <name>Task 4: Create feed.controller.ts (CRUD + Surat Jalan endpoint)</name>
  <files>server/src/modules/feed/feed.controller.ts, server/src/modules/feed/feed.routes.ts</files>
  <read_first>
    - server/src/modules/recordings/recordings.controller.ts (controller pattern)
    - server/src/modules/feed/feed.service.ts
  </read_first>
  <action>
Create `server/src/modules/feed/feed.controller.ts` with Elysia:
- Base: /api/feed
- POST /products - createFeedProduct (body: code, name, phase, proteinPercent, defaultUnit)
- GET /products - listFeedProducts (query: none)
- GET /products/:id - getFeedProduct
- PUT /products/:id - updateFeedProduct
- DELETE /products/:id - softDeleteFeedProduct
- POST /surat-jalan - createSuratJalan (body: plasmaId, feedProductId, suratJalanNumber, vendor, deliveryDate, vehicleNumber, driverName, totalZak, totalKg, notes)
- Error handlers for all feed errors
- Include requirePermission('feed.create') etc. guards

Create `server/src/modules/feed/feed.routes.ts`:
- Export feedController
</action>
  <verify>
    <automated>grep -n "feedController" server/src/modules/feed/feed.routes.ts</automated>
  </verify>
  <done>feed.controller.ts has all CRUD + Surat Jalan endpoints registered</done>
</task>

</tasks>

<verification>
- Feed product CRUD: create, list, get, update, delete all work
- Surat Jalan: creates movement record, increments stock atomically
- Transaction test: concurrent Surat Jalan calls don't cause race conditions
</verification>

<success_criteria>
- Feed product CRUD returns expected data
- Surat Jalan increases feed_stock.totalInKg correctly
- Movement record created with referenceType='surat_jalan'
- Stock query returns opening + in - out in both Zak and Kg
</success_criteria>

<output>
After completion, create `.planning/phases/06-feed-inventory/06-01-SUMMARY.md`
</output>