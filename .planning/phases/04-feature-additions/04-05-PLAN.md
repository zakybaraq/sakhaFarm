---
phase: 04-feature-additions
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/pages/rbac/RbacManager.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - RBAC already has toggle switch and edit button (verify event propagation)
  artifacts:
    - path: client/src/pages/rbac/RbacManager.tsx
      provides: Verify toggle and edit work correctly
      min_lines: 30
---

<objective>
Verify RBAC toggle and edit functionality (already implemented in UI).
</objective>

<context>
@.planning/phases/04-feature-additions/4-CONTEXT.md
@.planning/phases/04-feature-additions/4-RESEARCH.md
@client/src/pages/rbac/RbacManager.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify RBAC toggle has proper event propagation</name>
  <read_first>
    - client/src/pages/rbac/RbacManager.tsx (line 160-164 toggle, line 163 onChange)
  </read_first>
  <files>client/src/pages/rbac/RbacManager.tsx</files>
  <action>
1. Find the user toggle Switch at line 160-164:
   ```tsx
   <Switch
     size="small"
     checked={params.row.status === 'active'}
     onChange={() => handleToggleUserStatus(params.row.id)}
   />
   ```

2. Update onChange to stop propagation:
   ```tsx
   <Switch
     size="small"
     checked={params.row.status === 'active'}
     onChange={(e) => {
       e.stopPropagation()
       handleToggleUserStatus(params.row.id)
     }}
   />
   ```

3. Find the role edit button (line 119) and verify it has stopPropagation:
   ```tsx
   <IconButton size="small" onClick={(e) => {
     e.stopPropagation()
     handleEditRole(params.row)
   }}>
   ```

4. Find the user edit button (line 157) and verify it has stopPropagation:
   ```tsx
   <IconButton size="small" onClick={(e) => {
     e.stopPropagation()
     handleEditUser(params.row)
   }}>
   ```
  </action>
  <verify>
    <automated>grep -q "e.stopPropagation" client/src/pages/rbac/RbacManager.tsx</automated>
  </verify>
  <done>
    - All click handlers have e.stopPropagation() to prevent row selection
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→DataGrid | Untrusted user interaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | S | RBAC toggle | accept | Already implemented with state |
| T-04-10 | S | RBAC edit | accept | Already implemented |
</threat_model>

<verification>
- Build passes
- Toggle and edit button work correctly
</verification>

<success_criteria>
- RBAC has toggle switch for user status
- RBAC has edit button for roles and users
- Event propagation handled properly
</success_criteria>

<output>
After completion, create `.planning/phases/04-feature-additions/04-05-SUMMARY.md`
</output>