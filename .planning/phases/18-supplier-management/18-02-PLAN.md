---
phase: 18-supplier-management
plan: "02"
type: execute
wave: "2"
depends_on:
  - 18-supplier-management-01
files_modified:
  - client/src/pages/suppliers/Suppliers.tsx
  - client/src/pages/suppliers/SupplierModal.tsx
  - client/src/api/suppliers.ts
  - client/src/pages/feed/SuratJalanModal.tsx
  - client/src/components/layout/Sidebar.tsx
autonomous: true
requirements:
  - SUPPLIER-01
  - SUPPLIER-02
user_setup: []

must_haves:
  truths:
    - Supplier page displays in table with CRUD actions
    - Surat Jalan modal shows supplier dropdown
    - Sidebar has Supplier menu entry
  artifacts:
    - path: client/src/pages/suppliers/Suppliers.tsx
      provides: Supplier list page
      exports: Supplier page component
    - path: client/src/components/layout/Sidebar.tsx
      provides: Navigation menu
      contains: Supplier menu item
  key_links:
    - from: Suppliers.tsx
      to: api/suppliers.ts
      via: useQuery hooks
      pattern: listSuppliers|createSupplier|updateSupplier|deleteSupplier
---

<objective>
Frontend supplier management — supplier page and Surat Jalan integration.

Purpose: Create Supplier page with full CRUD UI and link to Surat Jalan.
Output: Working supplier page accessible to users.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-supplier-management/18-01-PLAN.md
@client/src/pages/feed-brands/FeedBrands.tsx
@client/src/pages/feed-brands/FeedBrandModal.tsx
@client/src/api/feed.ts
@client/src/components/layout/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create suppliers API client</name>
  <files>client/src/api/suppliers.ts</files>
  <action>
Create `client/src/api/suppliers.ts` following feed.ts patterns:

```typescript
import { apiClient } from "./index";

export interface Supplier {
  id: number;
  tenantId: number;
  code: string;
  name: string;
  contactPerson: string | null;
  phone: string;
  address: string | null;
  category: "feed" | "vitamin" | "medicine" | "other";
  isActive: number;
  createdAt: string;
  updatedAt: string;
}

export interface ListSuppliersParams {
  category?: string;
  isActive?: boolean;
}

export function listSuppliers(params?: ListSuppliersParams) {
  return apiClient.get<Supplier[]>("/suppliers", { params });
}

export function createSupplier(
  data: Omit<
    Supplier,
    "id" | "tenantId" | "isActive" | "createdAt" | "updatedAt"
  >,
) {
  return apiClient.post<Supplier>("/suppliers", data);
}

export function updateSupplier(id: number, data: Partial<Supplier>) {
  return apiClient.put<Supplier>(`/suppliers/${id}`, data);
}

export function deleteSupplier(id: number) {
  return apiClient.delete(`/suppliers/${id}`);
}
```

  </action>
  <verify>
    <automated>grep -l "export.*listSuppliers" client/src/api/suppliers.ts</automated>
  </verify>
  <done>Supplier API client functions exported</done>
</task>

<task type="auto">
  <name>Task 2: Create Supplier modal component</name>
  <files>client/src/pages/suppliers/SupplierModal.tsx</files>
  <action>
Create `client/src/pages/suppliers/SupplierModal.tsx` following FeedBrandModal.tsx pattern:

```typescript
import { useState } from 'react'
import { Dialog, DialogTitle, DialogContent, DialogActions, TextField, Button, FormControl, InputLabel, Select, MenuItem } from '@mui/material'
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { createSupplier, updateSupplier, type Supplier } from '../../api/suppliers'

interface SupplierModalProps {
  open: boolean
  onClose: () => void
  supplier?: Supplier | null
}

const CATEGORIES = ['feed', 'vitamin', 'medicine', 'other'] as const

export function SupplierModal({ open, onClose, supplier }: SupplierModalProps) {
  const [form, setForm] = useState({
    code: supplier?.code || '',
    name: supplier?.name || '',
    contactPerson: supplier?.contactPerson || '',
    phone: supplier?.phone || '',
    address: supplier?.address || '',
    category: supplier?.category || 'feed',
  })

  const queryClient = useQueryClient()
  const createMut = useMutation({ mutationFn: createSupplier, onSuccess: () => queryClient.invalidateQueries({ queryKey: ['suppliers'] }) })
  const updateMut = useMutation({ mutationFn: (data) => updateSupplier(supplier!.id, data), onSuccess: () => queryClient.invalidateQueries({ queryKey: ['suppliers'] }) })

  const handleSubmit = () => {
    if (supplier) updateMut.mutate(form)
    else createMut.mutate(form)
    onClose()
  }

  return (
    <Dialog open={open} onClose={onClose} maxWidth="sm" fullWidth>
      <DialogTitle>{supplier ? 'Edit' : 'Add'} Supplier</DialogTitle>
      <DialogContent>
        <TextField label="Code" value={form.code} onChange={(e) => setForm({ ...form, code: e.target.value })} fullWidth margin="normal" />
        <TextField label="Name" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} fullWidth margin="normal" />
        <TextField label="Contact Person" value={form.contactPerson} onChange={(e) => setForm({ ...form, contactPerson: e.target.value })} fullWidth margin="normal" />
        <TextField label="Phone" value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} fullWidth margin="normal" />
        <TextField label="Address" value={form.address} onChange={(e) => setForm({ ...form, address: e.target.value })} fullWidth margin="normal" multiline rows={2} />
        <FormControl fullWidth margin="normal">
          <InputLabel>Category</InputLabel>
          <Select value={form.category} label="Category" onChange={(e) => setForm({ ...form, category: e.target.value as any })}>
            {CATEGORIES.map((cat) => (
              <MenuItem key={cat} value={cat}>{cat}</MenuItem>
            ))}
          </Select>
        </FormControl>
      </DialogContent>
      <DialogActions>
        <Button onClick={onClose}>Cancel</Button>
        <Button onClick={handleSubmit} variant="contained">{supplier ? 'Update' : 'Create'}</Button>
      </DialogActions>
    </Dialog>
  )
}
```

  </action>
  <verify>
    <automated>grep -l "SupplierModal" client/src/pages/suppliers/SupplierModal.tsx</automated>
  </verify>
  <done>Supplier modal created with form fields</done>
</task>

<task type="auto">
  <name>Task 3: Create Supplier page component</name>
  <files>client/src/pages/suppliers/Suppliers.tsx</files>
  <action>
Create `client/src/pages/suppliers/Suppliers.tsx` following FeedBrands.tsx pattern:

```typescript
import { useState } from 'react'
import { Box, Typography, Button, IconButton } from '@mui/material'
import { Add, Edit, Delete, ToggleOn, ToggleOff } from '@mui/icons-material'
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import { listSuppliers, deleteSupplier } from '../../api/suppliers'
import { ResponsiveTable } from '../../components/common/ResponsiveTable'
import { SupplierModal } from './SupplierModal'

export function Suppliers() {
  const [modalOpen, setModalOpen] = useState(false)
  const [selectedSupplier, setSelectedSupplier] = useState(null)

  const { data: suppliers = [], isLoading } = useQuery({
    queryKey: ['suppliers'],
    queryFn: () => listSuppliers(),
  })

  const queryClient = useQueryClient()
  const deleteMut = useMutation({ mutationFn: deleteSupplier, onSuccess: () => queryClient.invalidateQueries({ queryKey: ['suppliers'] }) })

  const columns = [
    { field: 'code', header: 'Code', width: 100 },
    { field: 'name', header: 'Name', width: 150 },
    { field: 'contactPerson', header: 'Contact', width: 120 },
    { field: 'phone', header: 'Phone', width: 120 },
    { field: 'category', header: 'Category', width: 100 },
    {
      field: 'isActive',
      header: 'Status',
      width: 80,
      render: (row) => row.isActive ? 'Active' : 'Inactive'
    },
  ]

  const handleEdit = (supplier: any) => {
    setSelectedSupplier(supplier)
    setModalOpen(true)
  }

  const handleDelete = async (supplier: any) => {
    if (confirm('Delete this supplier?')) {
      deleteMut.mutate(supplier.id)
    }
  }

  return (
    <Box p={3}>
      <Box display="flex" justifyContent="space-between" alignItems="center" mb={2}>
        <Typography variant="h5">Suppliers</Typography>
        <Button startIcon={<Add />} variant="contained" onClick={() => { setSelectedSupplier(null); setModalOpen(true) }}>
          Add Supplier
        </Button>
      </Box>

      <ResponsiveTable
        data={suppliers}
        columns={columns}
        loading={isLoading}
        onEdit={handleEdit}
        onDelete={handleDelete}
      />

      <SupplierModal
        open={modalOpen}
        onClose={() => { setModalOpen(false); setSelectedSupplier(null) }}
        supplier={selectedSupplier}
      />
    </Box>
  )
}
```

  </action>
  <verify>
    <automated>grep -l "export.*Suppliers" client/src/pages/suppliers/Suppliers.tsx</automated>
  </verify>
  <done>Supplier page with table and CRUD actions</done>
</task>

<task type="auto">
  <name>Task 4: Update Surat Jalan modal with supplier dropdown</name>
  <files>client/src/pages/feed/SuratJalanModal.tsx</files>
  <action>
Update `client/src/pages/feed/SuratJalanModal.tsx` to add supplier dropdown (per D-08):

1. Import: `import { listSuppliers } from '../../api/suppliers'`
2. Add supplier query:
   ```typescript
   const { data: suppliers = [] } = useQuery({
     queryKey: ["suppliers", "feed"],
     queryFn: () => listSuppliers({ category: "feed" }),
   });
   ```
3. Add supplier dropdown in form:
   ```typescript
   <FormControl fullWidth margin="normal">
     <InputLabel>Supplier</InputLabel>
     <Select value={form.supplierId || ''} label="Supplier" onChange={(e) => {
       const sid = e.target.value as number
       setForm({ ...form, supplierId: sid, vendor: suppliers.find(s => s.id === sid)?.name || '' })
     }}>
       <MenuItem value=""><None></MenuItem>
       {suppliers.filter(s => s.category === 'feed').map(s => (
         <MenuItem key={s.id} value={s.id}>{s.name}</MenuItem>
       ))}
     </Select>
   </FormControl>
   ```
4. Add supplierId to form state

Note: Filter suppliers by category=feed. When supplier is selected, auto-populate vendor name field.
</action>
<verify>
<automated>grep "listSuppliers" client/src/pages/feed/SuratJalanModal.tsx</automated>
</verify>
<done>Surat Jalan modal includes supplier dropdown</done>
</task>

<task type="auto">
  <name>Task 5: Add sidebar entry for Suppliers</name>
  <files>client/src/components/layout/Sidebar.tsx</files>
  <action>
Update `client/src/components/layout/Sidebar.tsx` to add Supplier menu item (per D-09):

1. Import: `import { Suppliers } from '../../pages/suppliers/Suppliers'`
2. Add route:

   ```typescript
   { path: '/suppliers', label: 'Supplier', icon: <LocalShipping />, component: Suppliers }
   ```

3. Add menu item in sidebar list (after Feed or under Operations section):
   ```typescript
   { section: 'Operations', items: [
     { path: '/feed', icon: <Restaurant />, label: 'Feed Stock' },
     { path: '/suppliers', icon: <LocalShipping />, label: 'Supplier' },  // ADD THIS
   ]}
   ```

Use LocalShipping icon from @mui/icons-material.
</action>
<verify>
<automated>grep -i "supplier" client/src/components/layout/Sidebar.tsx</automated>
</verify>
<done>Sidebar has Supplier menu entry</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                |
| ---------- | -------------------------- |
| client→API | User input via form fields |

## STRIDE Threat Register

| Threat ID | Category               | Component     | Disposition | Mitigation Plan                                       |
| --------- | ---------------------- | ------------- | ----------- | ----------------------------------------------------- |
| T-18-04   | Tampering              | SupplierModal | mitigate    | Validate category enum via Select (not freeform text) |
| T-18-05   | Information Disclosure | Suppliers.tsx | accept      | Tenant-scoped at API level (service handles)          |

</threat_model>

<verification>
Frontend supplier page renders — verify manually after implementation
</verification>

<success_criteria>

- Supplier page shows table with CRUD actions
- Add/Edit modal works with all fields
- Supplier linked to Surat Jalan appears in dropdown
- Sidebar shows Supplier menu item
  </success_criteria>

<output>
After completion, create `.planning/phases/18-supplier-management/18-02-SUMMARY.md`
</output>
