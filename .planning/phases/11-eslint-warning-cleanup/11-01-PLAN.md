---
phase: 11-eslint-warning-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/layout/Navbar.tsx
  - client/src/components/layout/Sidebar.tsx
  - client/src/components/ui/ResponsiveTable.tsx
  - client/src/components/__tests__/DataGrid.test.tsx
  - client/src/contexts/AuthContext.tsx
  - client/src/pages/admin/AuditLog.tsx
  - client/src/pages/admin/UserModal.tsx
  - client/src/pages/admin/Users.tsx
  - client/src/pages/cycles/CycleModal.tsx
  - client/src/pages/cycles/Cycles.tsx
  - client/src/pages/units/UnitModal.tsx
  - client/src/pages/units/Units.tsx
  - client/src/pages/plasmas/PlasmaModal.tsx
  - client/src/pages/feed/SuratJalanModal.tsx
  - client/src/pages/feed/StockResume.tsx
  - client/src/pages/rbac/RbacManager.tsx
  - client/src/theme/index.ts
  - client/src/types/table.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 130 prettier formatting errors are resolved (0 errors from eslint)"
    - "All unused imports and dead code are removed (no unused variable warnings)"
    - "All intentionally unused variables are prefixed with underscore"
  artifacts:
    - path: "client/src/components/layout/Navbar.tsx"
      provides: "SakhaFarmLogo removed, _sidebarCollapsed prefix, prettier-clean"
    - path: "client/src/pages/cycles/Cycles.tsx"
      provides: "getStatusColor removed, selectedId removed, prettier-clean"
    - path: "client/src/pages/admin/Users.tsx"
      provides: "selectedId removed, prettier-clean"
  key_links:
    - from: "eslint --fix"
      to: "all .tsx/.ts files"
      via: "auto-formatting pass"
      pattern: "prettier/prettier"
---

<objective>
Auto-fix all 130 prettier formatting errors and manually resolve all 28 ESLint warnings from unused variables, dead code, and unused imports.

Purpose: Eliminate the bulk of ESLint problems (130 prettier errors + 20 unused-var warnings) in a single wave, leaving only the React hooks deps and type fixes for Plan 02.
Output: Client codebase passes `npx eslint src/` with only 8 remaining warnings (hooks deps, mixed exports, explicit any — handled in Plan 02).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-eslint-warning-cleanup/11-CONTEXT.md

<interfaces>
<!-- Key interfaces executor needs — extracted from codebase -->

From client/src/components/layout/Navbar.tsx (current props pattern):
```typescript
// Navbar receives sidebarCollapsed as prop but doesn't use it
interface NavbarProps {
  sidebarCollapsed: boolean;
  // ... other props
}
```

From client/src/pages/cycles/Cycles.tsx (dead code):
```typescript
// These are UNUSED after Phase 9 refactoring (Chip→Switch):
const [selectedId, setSelectedId] = useState<number | null>(null); // replaced by editId
function getStatusColor(status: string) { ... } // replaced by Switch component
```

From client/src/pages/admin/Users.tsx (dead code):
```typescript
const [selectedId, setSelectedId] = useState<number | null>(null); // replaced by editId
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-fix all 130 prettier formatting errors</name>
  <files>client/src/**/*.tsx, client/src/**/*.ts</files>
  <read_first>
    - client/eslint.config.js (understand ESLint config before running)
  </read_first>
  <action>
Per D-01: Run the ESLint auto-fix to eliminate all 130 prettier formatting errors in one pass.

1. Run: `cd client && npx eslint src/ --fix`
2. Verify output shows 0 errors (only warnings should remain)
3. Stage all auto-fixed files

This is safe because prettier errors are purely formatting. No logic changes.
  </action>
  <verify>
    <automated>cd client && npx eslint src/ 2>&1 | grep "error" | grep -v "warning" | head -5; echo "Exit: $?"</automated>
  </verify>
  <done>`npx eslint src/` shows 0 prettier/prettier errors. Only warnings remain (no-unused-vars, react-hooks, react-refresh).</done>
</task>

<task type="auto">
  <name>Task 2: Remove all dead code and unused imports</name>
  <files>
    - client/src/components/layout/Navbar.tsx
    - client/src/pages/cycles/Cycles.tsx
    - client/src/pages/admin/Users.tsx
    - client/src/pages/admin/AuditLog.tsx
    - client/src/pages/admin/UserModal.tsx
    - client/src/pages/units/UnitModal.tsx
    - client/src/pages/units/Units.tsx
    - client/src/pages/feed/SuratJalanModal.tsx
    - client/src/pages/feed/StockResume.tsx
    - client/src/pages/rbac/RbacManager.tsx
    - client/src/theme/index.ts
  </files>
  <read_first>
    - client/src/components/layout/Navbar.tsx (remove SakhaFarmLogo function ~line 25)
    - client/src/pages/cycles/Cycles.tsx (remove getStatusColor function ~line 82, remove selectedId useState ~line 27)
    - client/src/pages/admin/Users.tsx (remove selectedId useState ~line 15)
    - client/src/pages/admin/AuditLog.tsx (remove useEffect import ~line 1)
    - client/src/pages/admin/UserModal.tsx (remove useEffect import ~line 1 — verify it's truly unused before removing)
    - client/src/pages/units/UnitModal.tsx (remove Unit type import ~line 16)
    - client/src/pages/feed/SuratJalanModal.tsx (remove Typography import ~line 14)
    - client/src/pages/feed/StockResume.tsx (remove FormControl, InputLabel, Select, MenuItem imports ~lines 8-11)
    - client/src/pages/rbac/RbacManager.tsx (remove ApiUser type import ~line 33)
    - client/src/theme/index.ts (remove drawerClasses import ~line 2)
  </read_first>
  <action>
Per D-05: Remove all dead code and unused imports identified in CONTEXT.md.

**Remove unused functions:**
- `SakhaFarmLogo` function in Navbar.tsx (~line 25) — logo lives in Sidebar, this is never rendered
- `getStatusColor` function in Cycles.tsx (~line 82) — Phase 9 replaced Chip badges with Switch

**Remove dead useState:**
- `selectedId` useState in Cycles.tsx (~line 27) — replaced by `editId` in Phase 4. Remove the `const [selectedId, setSelectedId] = useState<number | null>(null)` line AND remove any remaining `setSelectedId(null)` calls (they're dead references)
- `selectedId` useState in Users.tsx (~line 15) — same pattern. Remove the useState AND any `setSelectedId(null)` calls

**Remove unused imports:**
- `useEffect` from AuditLog.tsx (~line 1) — verify no useEffect calls exist in the file, then remove from the import statement
- `useEffect` from UserModal.tsx (~line 1) — verify no useEffect calls exist in the file. If the file uses useEffect elsewhere, keep it. If not, remove from import.
- `Typography` from SuratJalanModal.tsx (~line 14) — remove from MUI import statement
- `FormControl`, `InputLabel`, `Select`, `MenuItem` from StockResume.tsx (~lines 8-11) — remove from MUI import statement
- `ApiUser` type import from RbacManager.tsx (~line 33) — remove the import
- `Unit` type import from UnitModal.tsx (~line 16) — remove the import
- `drawerClasses` from theme/index.ts (~line 2) — remove the import

IMPORTANT: When removing imports from multi-line import statements, remove only the specific identifier, not the entire import. For example, if `Typography` is one of several MUI imports, remove just `Typography,` from the import block.
  </action>
  <verify>
    <automated>cd client && npx eslint src/ 2>&1 | grep -c "warning" | xargs -I{} bash -c 'if [ {} -le 10 ]; then echo "PASS: {} warnings or fewer"; else echo "FAIL: {} warnings remain (expected ≤10)"; exit 1; fi'</automated>
  </verify>
  <done>All dead code removed. ESLint shows no unused-variable warnings for the removed identifiers. Only remaining warnings are: prefixed underscore vars (D-06), catch blocks (D-02), hooks deps (D-03), mixed exports (D-04), explicit any (D-08), and loading states (D-07).</done>
</task>

<task type="auto">
  <name>Task 3: Prefix intentionally unused variables with underscore</name>
  <files>
    - client/src/components/layout/Navbar.tsx
    - client/src/pages/admin/AuditLog.tsx
    - client/src/pages/admin/UserModal.tsx
    - client/src/pages/cycles/CycleModal.tsx
    - client/src/pages/units/UnitModal.tsx
    - client/src/pages/plasmas/PlasmaModal.tsx
    - client/src/pages/feed/SuratJalanModal.tsx
    - client/src/pages/rbac/RbacManager.tsx
  </files>
  <read_first>
    - client/src/components/layout/Navbar.tsx (prefix sidebarCollapsed ~line 81)
    - client/src/pages/admin/AuditLog.tsx (prefix noApiFilters ~line 67, hasFilters ~line 195)
    - client/src/pages/admin/UserModal.tsx (prefix err in catch ~line 95)
    - client/src/pages/cycles/CycleModal.tsx (prefix err in catch ~line 117)
    - client/src/pages/units/UnitModal.tsx (prefix err in catch ~line 90)
    - client/src/pages/plasmas/PlasmaModal.tsx (prefix err in catch ~line 110)
    - client/src/pages/feed/SuratJalanModal.tsx (prefix err in catch ~line 76)
  </read_first>
  <action>
Per D-02, D-06, D-09: Prefix intentionally unused variables with `_` to satisfy ESLint's `no-unused-vars` rule.

**Catch block parameters (D-02):**
- UnitModal.tsx ~line 90: change `catch (err)` → `catch (_err)`
- CycleModal.tsx ~line 117: change `catch (err)` → `catch (_err)`
- PlasmaModal.tsx ~line 110: change `catch (err)` → `catch (_err)`
- SuratJalanModal.tsx ~line 76: change `catch (err)` → `catch (_err)`
- UserModal.tsx ~line 95: change `catch (err)` → `catch (_err)`

**Forward-looking variables (D-06):**
- AuditLog.tsx ~line 67: change `const noApiFilters` → `const _noApiFilters`
- AuditLog.tsx ~line 195: change `const hasFilters` → `const _hasFilters`

**Navbar prop (D-09):**
- Navbar.tsx ~line 81: In the function parameters, change `sidebarCollapsed` → `_sidebarCollapsed` in the destructuring. For example: `{ _sidebarCollapsed, ...otherProps }` or if it's `(props)` then destructure with underscore.
  </action>
  <verify>
    <automated>cd client && npx eslint src/ 2>&1 | grep "no-unused-vars" | grep -v "rolesLoading\|usersLoading" | head -5; echo "If empty: PASS"</automated>
  </verify>
  <done>All catch parameters, forward-looking vars, and unused props are underscore-prefixed. Only remaining ESLint warnings are: react-hooks/exhaustive-deps (2), react-refresh/only-export-components (1), no-explicit-any (1), and rolesLoading/usersLoading unused-vars (2) — all handled in Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure code quality phase — no security-relevant changes. All changes are lint fixes, dead code removal, and formatting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | I | catch blocks | accept | Prefixing with `_` does not suppress actual errors — exceptions are still caught, just the parameter is explicitly unused |
| T-11-02 | D | dead code removal | accept | Removing unused functions and variables reduces attack surface — net positive |
</threat_model>

<verification>
1. `cd client && npx eslint src/ --fix` — 0 errors expected
2. `cd client && npx eslint src/` — at most 8 warnings (hooks deps 2, mixed exports 1, explicit any 1, loading states 2, remaining items 2)
3. `cd client && npx tsc --noEmit` — 0 TypeScript errors (dead code removal should not break types)
</verification>

<success_criteria>
- `npx eslint src/` in client directory shows 0 errors (prettier/prettier)
- At most 8 warnings remain (all covered by Plan 02)
- No TypeScript compilation errors introduced
- All files from the warning inventory in CONTEXT.md have been addressed
</success_criteria>

<output>
After completion, create `.planning/phases/11-eslint-warning-cleanup/11-01-SUMMARY.md`
</output>
