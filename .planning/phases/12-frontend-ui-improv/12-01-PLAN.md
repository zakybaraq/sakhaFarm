---
phase: 12-frontend-ui-improv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/layout/Sidebar.tsx
autonomous: true
requirements: [UI-FIX-1, UI-FIX-2, UI-FIX-3, UI-FIX-4]

must_haves:
  truths:
    - "Toggle button appears in same row as logo (not below)"
    - "Divider aligns with Navbar bottom edge"
    - "Menu items show singular names (Unit, Plasma, Cycle, Recording)"
    - "No duplicate Users menu (RBAC already contains user management)"
  artifacts:
    - path: "client/src/components/layout/Sidebar.tsx"
      provides: "Fixed sidebar layout with proper alignment"
      contains: "flex.*alignItems.*center"
  key_links:
    - from: "client/src/components/layout/Sidebar.tsx"
      to: "client/src/components/layout/Navbar.tsx"
      via: "visual alignment"
      pattern: "drawer.*width"
---

<objective>
Fix sidebar layout issues in Phase 1 of v1.1 milestone:
- Put toggle button in same row as logo (not below)
- Fix divider alignment to match Navbar bottom
- Change menu names from plural to singular
- Remove duplicate Users menu item

Purpose: Professional UI alignment matching Material Design standards
Output: One modified Sidebar.tsx file
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-frontend-ui-improv/12-CONTEXT.md
@.planning/phases/12-frontend-ui-improv/12-RESEARCH.md

# Current state from Sidebar.tsx (read before modifying):
Lines 72-127: Header section with logo, toggle, divider
Lines 43-55: Menu items array with plural names
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix logo + toggle button alignment (same row)</name>
  <files>client/src/components/layout/Sidebar.tsx</files>
  <read_first>
    - client/src/components/layout/Sidebar.tsx (lines 72-127)
  </read_first>
  <action>
    Modify the header Box (lines 72-121) to include toggle button inline:

    1. Find the header Box container (line 72-121)
    2. Move the toggle button (currently lines 122-126) INTO the header Box
    3. Use flexbox with justifyContent: 'space-between' to push toggle to right edge

    Current structure (WRONG):
    ```typescript
    <Box header>logo + text</Box>
    <Box toggle button />
    <Divider />
    ```

    Target structure (CORRECT):
    ```typescript
    <Box sx={{display:'flex', justifyContent:'space-between', alignItems:'center', ...}}>
      <Box>logo + text</Box>
      <IconButton toggle />
    </Box>
    <Divider />
    ```
  </action>
  <verify>
    <automated>grep -n "justifyContent.*space-between" client/src/components/layout/Sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx contains flexbox with justifyContent: 'space-between' in header
    - Toggle button is inside same Box as logo (not separate)
  </acceptance_criteria>
  <done>Toggle button and logo appear in same horizontal row</done>
</task>

<task type="auto">
  <name>Task 2: Fix divider alignment to match Navbar</name>
  <files>client/src/components/layout/Sidebar.tsx</files>
  <read_first>
    - client/src/components/layout/Sidebar.tsx (lines 127)
    - client/src/components/layout/Navbar.tsx (height section)
  </read_first>
  <action>
    1. The Divider (line 127 in original) should remain at same vertical position
    2. Ensure header Box has explicit height matching Navbar height
    3. After moving toggle button, verify Divider is at bottom of header section

    Add: `height: 64` (or match Navbar height) to header Box sx prop
  </action>
  <verify>
    <automated>grep -A5 "Divider" client/src/components/layout/Sidebar.tsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Divider appears immediately after header content (logo + toggle)
    - Header has explicit height for consistent alignment
  </acceptance_criteria>
  <done>Divider aligns with Navbar bottom edge</done>
</task>

<task type="auto">
  <name>Task 3: Change menu names to singular</name>
  <files>client/src/components/layout/Sidebar.tsx</files>
  <read_first>
    - client/src/components/layout/Sidebar.tsx (lines 43-55, menuItems array)
  </read_first>
  <action>
    Update menuItems array text values from PLURAL to SINGULAR:

    Line 45: 'Units' → 'Unit'
    Line 46: 'Plasmas' → 'Plasma'
    Line 47: 'Cycles' → 'Cycle'
    Line 48: 'Recordings' → 'Recording'
    Line 49: 'Feed Stock' → 'Feed Stock' (already singular)
    Line 50: 'Performance' → 'Performance' (already singular)
    Line 51: 'Stock Resume' → 'Stock Resume' (already singular)
    Line 52: 'Audit Log' → 'Audit Log' (already singular)
  </action>
  <verify>
    <automated>grep -E "text: '(Unit|Plasma|Cycle|Recording)'" client/src/components/layout/Sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - menuItems contains 'Unit' not 'Units'
    - menuItems contains 'Plasma' not 'Plasmas'
    - menuItems contains 'Cycle' not 'Cycles
  </acceptance_criteria>
  <done>Menu displays singular names</done>
</task>

<task type="auto">
  <name>Task 4: Remove duplicate Users menu item</name>
  <files>client/src/components/layout/Sidebar.tsx</files>
  <read_first>
    - client/src/components/layout/Sidebar.tsx (line 54)
  </read_first>
  <action>
    Remove line 54 from menuItems array:
    ```typescript
    { text: 'Users', icon: <PeopleIcon />, path: '/users', permission: 'user.read' },
    ```
    Rationale: Users already accessible via RBAC menu (line 53)
  </action>
  <verify>
    <automated>grep -n "'Users'" client/src/components/layout/Sidebar.tsx || echo "No Users found - REMOVED"</automated>
  </verify>
  <acceptance_criteria>
    - No 'Users' entry in menuItems array
    - RBAC menu still present (line 53: 'RBAC')
  </acceptance_criteria>
  <done>Users menu item removed</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 5: Verify sidebar visual alignment</name>
  <what_built>Modified Sidebar.tsx with fixed layout</what_built>
  <how_to_verify>
    1. Run the app: `cd client && bun run dev`
    2. Open browser: http://localhost:5173
    3. Check sidebar:
       - Toggle button appears RIGHT of logo (same row, not below logo)
       - Divider line is horizontal, aligned with Navbar bottom
       - Menu shows: Unit, Plasma, Cycle, Recording (singular names)
       - No duplicate "Users" menu item
    4. Toggle collapse button → sidebar collapses properly
  </how_to_verify>
  <resume_signal>Type "approved" or describe any issues</resume_signal>
</task>

</tasks>

<verification>
Run these automated checks:
```bash
# Check flexbox alignment
grep -n "justifyContent.*space-between" client/src/components/layout/Sidebar.tsx

# Check singular names
grep -E "text: '(Unit|Plasma|Cycle|Recording)'" client/src/components/layout/Sidebar.tsx

# Check no duplicate Users
grep "'Users'" client/src/components/layout/Sidebar.tsx || echo "Users removed"
```
</verification>

<success_criteria>
- [ ] Toggle button in same row as logo (flexbox with space-between)
- [ ] Divider at header bottom
- [ ] Menu shows singular names: Unit, Plasma, Cycle, Recording
- [ ] No duplicate Users menu item
- [ ] Human verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-ui-improv/12-01-PLAN-SUMMARY.md`
</output>