# 10-03-PLAN.md — Security Tests

## Plan Overview
- **Phase**: 10 - Testing, Cybersecurity & Documentation
- **Task**: Security Tests (rate limiting, tenant isolation, auth bypass)
- **Agent**: build (execute directly)
- **Prerequisites**: 10-CONTEXT.md

## Tasks

### T1: Create security.test.ts
**Description**: Core security vulnerability tests  
**Location**: `tests/unit/security.test.ts` (new file)  
**Pattern**: Vitest unit tests  
**Steps**:
1. Create test file with mocks
2. Test rate limiting 429 response
3. Test SQL injection prevention
4. Test auth bypass prevention
5. Run tests

### T2: Add tenant isolation tests
**Description**: Verify cross-tenant access blocked  
**Location**: `tests/unit/security.test.ts`  
**Steps**:
1. Test query with wrong tenant_id fails
2. Test middleware blocks cross-tenant
3. Test session tenant validation

### T3: Add SQL injection tests
**Description**: Verify parameterized queries  
**Location**: `tests/unit/security.test.ts`  
**Steps**:
1. Test raw SQL blocked via Drizzle
2. Test parameter binding works
3. Test malicious input sanitized

### T4: Add auth bypass tests
**Description**: Verify unauthorized access blocked  
**Location**: `tests/unit/security.test.ts`  
**Steps**:
1. Test no token returns 401
2. Test invalid token returns 401
3. Test expired token returns 401

### T5: Verify CSP configuration
**Description**: Verify security headers block inline scripts  
**Steps**:
1. Read security-headers.ts config
2. Verify CSP policy
3. Document test approach

## Test Pattern
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

const { mockRedis } = vi.hoisted(() => ({
  mockRedis: {
    incr: vi.fn().mockResolvedValue(1),
    expire: vi.fn().mockResolvedValue(true),
  },
}))

vi.mock('../../server/src/config/redis', () => ({
  redis: mockRedis,
}))

describe('rate limiting', () => {
  it('should return 429 when limit exceeded', async () => {
    mockRedis.incr.mockResolvedValue(101) // Above 100 limit
    // Test implementation
  })
})
```

## Test Commands
```bash
bun run test
```

## Verification
- [ ] Rate limit test returns 429
- [ ] Tenant isolation blocks access
- [ ] SQL injection blocked
- [ ] Auth bypass returns 401

## Deliverables
- New `tests/unit/security.test.ts`

---
*Plan created: 2026-04-17*