# 10-02-PLAN.md — Client Component Tests

## Plan Overview
- **Phase**: 10 - Testing, Cybersecurity & Documentation
- **Task**: Client Component Tests
- **Agent**: build (execute directly)
- **Prerequisites**: 10-01-PLAN.md (tests pass)

## Tasks

### T1: Create DataGrid test setup
**Description**: Set up React Testing Library for client tests  
**Location**: `client/src/components/__tests__/` (new directory)  
**Pattern**: Follow Vitest + React Testing Library patterns  
**Steps**:
1. Install @testing-library/react if needed
2. Create test setup file
3. Configure vitest for jsdom

### T2: Create DataGrid.test.tsx
**Description**: Test Units, Plasmas DataGrid components  
**Location**: `client/src/components/__tests__/DataGrid.test.tsx` (new file)  
**Pattern**: Component render + interaction tests  
**Steps**:
1. Test DataGrid renders
2. Test rows display correctly
3. Test pagination works
4. Run tests

### T3: Create UnitModal.test.tsx
**Description**: Test Unit modal component  
**Location**: `client/src/components/__tests__/UnitModal.test.tsx` (new file)  
**Steps**:
1. Test modal opens/closes
2. Test form validation (Zod)
3. Test submit handler
4. Run tests

### T4: Create PlasmaModal.test.tsx
**Description**: Test Plasma modal component  
**Location**: `client/src/components/__tests__/PlasmaModal.test.tsx` (new file)  
**Steps**:
1. Test modal opens/closes
2. Test form validation
3. Test submit handler
4. Run tests

## Test Pattern
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'

describe('DataGrid', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  it('should render rows', () => {
    render(<DataGrid rows={mockRows} />)
    expect(screen.getByText('Unit 1')).toBeInTheDocument()
  })
})
```

## Test Commands
```bash
cd client && bun run test
```

## Verification
- [ ] All component tests pass
- [ ] No console errors

## Deliverables
- New `client/src/components/__tests__/` directory
- DataGrid.test.tsx
- UnitModal.test.tsx
- PlasmaModal.test.tsx

---
*Plan created: 2026-04-17*